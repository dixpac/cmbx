<%= form_with(model: manifacturer) do |form| %>
  <% if manifacturer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(manifacturer.errors.count, "error") %> prohibited this manifacturer from being saved:</h2>

      <ul>
        <% manifacturer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
